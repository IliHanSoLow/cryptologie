[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "IO_08_02"
version = "0.1.0"
description = "Bauer W08"
authors = [{name = "IO"}]
license = {text = "MIT"}
